// Re-export theme types from lib/theme
export type { ColorTheme, ThemeMode } from '@/lib/theme'

// App-specific types

// Add any app-specific types here

// Language types
export type Language = "en" | "fr"